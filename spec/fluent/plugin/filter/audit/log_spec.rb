RSpec.describe Fluent::Plugin::Filter::Audit::Log do
  it "has a version number" do
    expect(Fluent::Plugin::Filter::Audit::Log::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
